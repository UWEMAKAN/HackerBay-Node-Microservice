version: '3'

services:
  api:
    container_name: service_jsonpatch_thumbnail
    build: ./
    working_dir: /app
    command: /bin/bash
    ports:
      - "3000:3000"
    volumes:
      - ./:/usr/src/api
